// Site entry point. Real UI (Leaflet + Leaflet.draw + sidebar + KML download)
// lands in phase 6.
import { placeholder } from "@rural-jet-lag/core";
document.getElementById("app").textContent =
  `Scaffold. Core loaded: ${placeholder}.`;
